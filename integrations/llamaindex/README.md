# WCP tools for LlamaIndex

**Worked-example domains:** field-research, healthcare-logistics.

```bash
pip install llama-index-core wcp-sdk
```

Use `make_wcp_tools(agent)` to obtain a list of `FunctionTool` instances ready to attach to a LlamaIndex agent.
